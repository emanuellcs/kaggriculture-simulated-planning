# Benchmark Reports

This directory is the default output location for `scripts/bench.py` reports.
Each report records the date, seed set, step cap, and per-opponent win rates so
results remain reproducible and comparable across changes.
